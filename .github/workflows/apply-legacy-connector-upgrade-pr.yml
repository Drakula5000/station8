name: Apply loaded legacy connector upgrade

on:
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.event.pull_request.head.ref == 'fix/connector-drag-office-drop-ux' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/connector-drag-office-drop-ux
          fetch-depth: 0
      - name: Apply legacy connector upgrade patch
        run: python ops/apply_legacy_connector_upgrade_fix.py
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
        working-directory: frontend
      - run: npm run lint
        working-directory: frontend
      - run: npm test
        working-directory: frontend
      - run: npm run build
        working-directory: frontend
      - name: Commit verified legacy upgrade
        run: |
          rm -f ops/apply_legacy_connector_upgrade_fix.py
          rm -f .github/workflows/apply-legacy-connector-upgrade-pr.yml
          git config user.name "Station 8 Fix Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "fix: upgrade bound legacy connectors after load"
          git push origin HEAD:fix/connector-drag-office-drop-ux
