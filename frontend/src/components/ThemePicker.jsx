// ThemePicker is no longer used — Aurora is the only theme.
// Kept as a stub so any stale imports don't break the build.

export const BOARD_THEMES = []
export const VISITOR_RANDOM_POOL = []

export function ThemePicker() {
  return null
}
